.DS_Store
*.swp
*.swo
*~
repos/

# MCP vendored runtimes + secrets (pnpm-lock.yaml IS committed for reproducibility)
node_modules/
.venv/
.env

# Machine-local skill state (e.g. email-cleanup per-mailbox status files) — never committed
var/
