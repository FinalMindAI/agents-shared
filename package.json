{
  "name": "agents-shared-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Vendored, version-pinned node MCP server runtimes for agents-shared. Launched locally from node_modules/.bin via mcp/with-env instead of `npx -y` pulling latest each launch.",
  "dependencies": {
    "@azure/mcp": "3.0.0-beta.34",
    "@shinzolabs/gmail-mcp": "1.7.4",
    "slack-mcp-server": "1.3.0"
  }
}
